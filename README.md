# Uptime Monitor MVP

A lightweight, full-stack application designed to monitor the health of websites in real-time. It provides a simple dashboard to register URLs and automatically tracks their operational status (UP/DOWN) and response times.

---

## 🚀 1-Line Setup
To launch the entire ecosystem locally, ensure you have Docker installed and run the following command in the project root:

```bash
docker compose up --build